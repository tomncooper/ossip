<!DOCTYPE html>

<head>
    <title>Kafka Improvement Proposals</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h1>Kafka Improvement Proposals (KIPs)</h1>
    <p><a href="index.html">Back to OSSIP</a></p>
    <p>GitHub Repository: <a href="https://github.com/tomncooper/ossip">tomncooper/ossip</a></p>
    <p>Last Updated: {{ date }}</p>
    <h2>KIPs Under Discussion</h2>
    <table>
        <tr>
            <th>KIP</th>
            <th>Description</th>
            <th>Status</th>
            <th>+1</th>
            <th>0</th>
            <th>-1</th>
            <th>Created by</th>
            <th>Age</th>
        </tr>
        {% for kip in kip_status %}
        <tr>
            <td><a href={{ kip['url'] }}>{{ kip['id'] }}</a></td>
            <td>{{ kip['text'] }}
            <td style="background-color:{{ kip['status'].text }};">
                <div class="tooltip">&nbsp;
                    <span class="tooltiptext">Mentioned within the last {{ kip['status'].duration.days }} days</span>
                </div>
            </td>
            {% if kip["+1"] %}
            <td style="background-color:green;">
                <div class="tooltip">{{ kip["+1"]|length }}
                    <span class="tooltiptext">
                        {% for name in kip["+1"] %}
                        {{ name }}<br>
                        {% endfor %}
                    </span>
                </div>
            </td>
            {% else %}
            <td>{{ kip["+1"]|length }}</td>
            {% endif %}
            {% if kip["0"] %}
            <td style="background-color:yellow;">
                <div class="tooltip">{{ kip["0"]|length }}
                    <span class="tooltiptext">
                        {% for name in kip["0"] %}
                        {{ name }}<br>
                        {% endfor %}
                    </span>
                </div>
            </td>
            {% else %}
            <td>{{ kip["0"]|length }}</td>
            {% endif %}
            {% if kip["-1"] %}
            <td style="background-color:red;">
                <div class="tooltip">{{ kip["-1"]|length }}
                    <span class="tooltiptext">
                        {% for name in kip["-1"] %}
                        {{ name }}<br>
                        {% endfor %}
                    </span>
                </div>
            </td>
            {% else %}
            <td>{{ kip["-1"]|length }}</td>
            {% endif %}
            <td>{{ kip["created_by"] }}</td>
            <td>{{ kip["age"] }}</td>
        </tr>
        {% endfor %}
    </table>
</body>

</html>
