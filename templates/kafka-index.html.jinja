<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kafka Improvement Proposals</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h1>Kafka Improvement Proposals (KIPs)</h1>
    <div class="nav-bar">
        <div class="nav-item"><a href="index.html">Back to OSSIP</a></div>
        <div class="nav-item">GitHub Repository: <a href="https://github.com/tomncooper/ossip">tomncooper/ossip</a></div>
        <div class="nav-item">Last Updated: {{ date }}</div>
    </div>
    
    <!-- Filter Controls -->
    <div id="filter-controls"></div>
    
    <table>
        <thead>
        <tr>
            <th>KIP</th>
            <th>Description</th>
            <th>State</th>
            <th>Status</th>
            <th>+1</th>
            <th>0</th>
            <th>-1</th>
            <th>Created by</th>
            <th>Age</th>
            <th>More Info</th>
        </tr>
        </thead>
        <tbody>
        {% for kip in kip_status %}
        <tr data-state="{{ kip['state']|e }}" data-created-by="{{ kip['created_by']|e }}">
            <td><a href={{ kip['url'] }}>{{ kip['id'] }}</a></td>
            <td>{{ kip['text'] }}</td>
            <td>{{ kip['state'] }}</td>
            {% if kip['status'] %}
            <td style="background-color:{{ kip['status'].text }};">
                <div class="tooltip">&nbsp;
                    <span class="tooltiptext">{% if kip['last_mention_age'] %}Last mentioned {{ kip['last_mention_age'] }} ago{% else %}Never mentioned in mailing lists{% endif %}</span>
                </div>
            </td>
            {% else %}
            <td style="text-align: center; font-size: 1.5em;">{{ kip['emoji'] }}</td>
            {% endif %}
            {% if kip["+1"] %}
            <td class="vote-positive">
                <div class="tooltip">{{ kip["+1"]|length }}
                    <span class="tooltiptext">
                        {% for voter in kip["+1"] %}
                        {{ voter.name }}<br><em>{{ voter.timestamp }}</em><br>
                        {% endfor %}
                    </span>
                </div>
            </td>
            {% else %}
            <td>{{ kip["+1"]|length }}</td>
            {% endif %}
            {% if kip["0"] %}
            <td class="vote-neutral">
                <div class="tooltip">{{ kip["0"]|length }}
                    <span class="tooltiptext">
                        {% for voter in kip["0"] %}
                        {{ voter.name }}<br><em>{{ voter.timestamp }}</em><br>
                        {% endfor %}
                    </span>
                </div>
            </td>
            {% else %}
            <td>{{ kip["0"]|length }}</td>
            {% endif %}
            {% if kip["-1"] %}
            <td class="vote-negative">
                <div class="tooltip">{{ kip["-1"]|length }}
                    <span class="tooltiptext">
                        {% for voter in kip["-1"] %}
                        {{ voter.name }}<br><em>{{ voter.timestamp }}</em><br>
                        {% endfor %}
                    </span>
                </div>
            </td>
            {% else %}
            <td>{{ kip["-1"]|length }}</td>
            {% endif %}
            <td>{{ kip["created_by"] }}</td>
            <td>{{ kip["age"] }}</td>
            <td><a href=kips/KIP-{{ kip['id'] }}.html>info</a></td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

    <!-- Filter JavaScript -->
    <script src="assets/table-filter.js"></script>
    <script>
        TableFilter.init({
            tableSelector: 'table',
            filterContainerId: 'filter-controls',
            columns: [
                { id: 'state', label: 'State', dataAttr: 'data-state' },
                { id: 'created-by', label: 'Created By', dataAttr: 'data-created-by' }
            ]
        });
    </script>
</body>

</html>